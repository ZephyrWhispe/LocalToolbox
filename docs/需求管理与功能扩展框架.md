# 需求管理与功能扩展框架

> 「LocalToolbox」的功能迭代运行规范：从需求收集、优先级评估、模块化开发、
> 版本发布到文档同步的全流程框架。
> 配套文档：[功能设计文档.md](./功能设计文档.md)、[技术文档.md](./技术文档.md)、[功能审查与整合记录.md](./docs/archive/功能审查与整合记录.md)
> 版本：v1.0（2026-09-08）　·　适用平台：Windows only

---

## 一、目标与原则

让后续功能迭代**可收集、可评估、可排期、可落地、可回溯**：

| 原则 | 含义 |
|------|------|
| 单入口 | 所有需求进同一「需求池」，先评估后排期，杜绝口头直达代码 |
| 增量优先 | 优先纯增量（core + bridge + page 三段式），少动既有契约 |
| 契约稳定 | js_api 返回 `{ok,data|err}` 不可变；新增方法用 `页面_动作` 命名不破坏旧版 |
| 测试随行 | 每个功能必须带自动化测试，回归基线只增不减 |
| 文档同步 | 代码合并前必须同步设计/技术/变更记录三件套 |

---

## 二、需求收集流程

### 2.1 渠道

| 渠道 | 说明 | 去向 |
|------|------|------|
| 用户反馈 | 群聊 / 工单 / 直接对话 | 需求卡片 → 需求池 |
| 自审发现 | 代码审查、冒烟测试、性能观察发现的缺口 | 需求卡片（附代码位置） |
| 竞品/趋势 | 同类工具功能观察 | 需求卡片（标注来源） |
| 技术债 | 重构、升级、安全加固 | 需求卡片（类型=技术债） |

### 2.2 需求卡片模板（必填）

每一条需求用统一卡片登记（存 `docs/requests/` 下，或需求池表格）：

```
---
id: REQ-2026-009        # 年度流水号，唯一
标题: 剪贴板支持同步文件类型
类型: 功能 | 增强 | 技术债 | 修复
提出人: xxx      日期: 2026-09-08      渠道: 用户反馈
---

## 用户场景
（谁、在什么情况下、想做什么、为什么）

## 期望行为
（具体可验证的描述；尽量给出验收口径）

## 现状
（当前系统行为 / 相关代码位置）

## 影响面
（涉及模块、端口、配置、前端页面；未知则标注待调研）

## 关联
（依赖的需求 / 被依赖的需求 / 相关缺陷编号）
```

### 2.3 流程

```
收集 → 登记卡片 → 评审会（周/里程碑）→ 评估分级（见 §三）
     → 排期（版本）→ 开发 → 测试 → 文档 → 发布 → 复盘（回收数据更新卡片）
```

- 评审会产出：每张卡片 → `采纳 / 暂缓 / 拒绝`（拒绝需写明原因并归档）；
- 采纳卡片进入版本规划；暂缓卡片留在需求池等待下一次评估。

---

## 三、功能优先级评估机制

### 3.1 四级优先级（P0–P3）

| 级别 | 定义 | 示例 | 处理 |
|------|------|------|------|
| **P0** | 核心链路缺失或阻断性缺陷 | 传输引擎、配对加密、单实例 | 立即排期，当版本必做 |
| **P1** | 高频场景必备，无则体验硬伤 | 托盘常驻、截图标注、图片同步 | 次版本完成 |
| **P2** | 有用但可绕过 | 队列持久化、全局热键、HUD | 待资源排期 |
| **P3** | 远期/低频/依赖外部条件 | 密码管理、虚拟挂载、TLS | 需求池观察 |

### 3.2 评分模型（RICE 简版）

评估会为每张 P0/P1 候选卡打 4 项分（1–5），加权求和排序：

```
R(影响)  ×2 + I(信心) ×1 + C(成本反向) ×2 + E(风险反向) ×1 = 总分
```

| 维度 | 打分要点 |
|------|----------|
| R 影响 | 覆盖用户比例 × 频率 × 缺它有多痛 |
| I 信心 | 对预期收益的把握（有无原型/数据） |
| C 成本 | 估算工作量、涉及模块数、是否有外部依赖 |
| E 风险 | 破坏既有契约概率、安全面扩大、平台兼容 |

> 排序结果仅作参考，最终由评审会裁决（技术债可豁免模型）。

### 3.3 发布门禁（每版本必须全过）

- 新功能：单测 ≥1 例覆盖主链路 + 冒烟测试无新增 toast 错误；
- 回归：`test_transfer.py` / `test_pairing.py` / `test_logger.py` / `test_ui_smoke.py` 全过；
- 文档：功能设计文档（功能说明）、技术文档（js_api/协议/CHANGELOG）已同步；
- 契约：无既有 js_api 方法签名破坏（新增 OK，改坏不 OK）。

---

## 四、模块化设计规范

### 4.1 分层架构（三段式）

```
app/core/*.py        业务内核：纯 Python、与 UI 无关、可独立测试
app/bridge/*_api.py  桥接 Mixin：暴露 js_api，仅做参数校验/编排/错误转中文
webui/js/pages/*.js  前端页面：App.registerPage 注册，事件监听放脚本顶层 IIFE
```

新增功能一律按此三段式切分，禁止跨层耦合：

| 允许 | 禁止 |
|------|------|
| core 调 core（如 transfer 调 pairing） | core 调 bridge / webui |
| bridge 调 core | bridge 调 UI DOM |
| page 调 bridge js_api | page 直接 import Python |

### 4.2 新增模块检查清单

- [ ] `core` 模块有清晰职责边界与命名空间（logger 命名 `app.<module>`）；
- [ ] 桥接类命名 `XxxApi`，在 `bridge.py` 继承链挂载 `_init_xxx()`；
- [ ] js_api 方法名 `页面_动作`，返回 `{ok:True,data:…}` / `{ok:False,err:"中文"}`；
- [ ] pywebview 6 限制：方法用普通 `def`（**禁止 async def**），参数仅 JSON 可序列化类型；
- [ ] 耗时/进度任务：后台线程 + `self.emit(event, payload)` 推送，不在 js_api 线程阻塞 UI；
- [ ] 文件/目录选择用 `window.create_file_dialog` + `webview.OPEN_DIALOG/FOLDER_DIALOG` 常量；
- [ ] 新配置项：`config.py` 白名单 + 默认值 + 文档表（功能设计/技术文档 §三）；
- [ ] 新端口：登记到端口分配表；防火墙放行联动（现有 `firewall.py` 模式）；
- [ ] 新页面：`App.registerPage({id,title,icon,group,mount,show})`，group ∈ 协作/共享服务/工具/系统；
- [ ] 安全默认：路径防穿越（`_safe_rel`/realpath 前缀校验）、哈希校验、日志不落敏感参数；
- [ ] 配套单测 `test_<module>.py`；需真实窗口的走 `test_ui_smoke.py` 探针模式。

### 4.3 命名与契约

- js_api：`页面_动作`（clip_copy / xfer_send / shot_capture / tool_hash …）；
- 事件：`xxx_event` 语义化命名（`xfer_progress` / `clip_log` …），负载为 JSON 对象；
- 配置：`snake_case`，布尔默认保守（安全项默认关）；
- 前端：页面文件 `pages/<id>.js`，组件函数小写驼峰，全局命名空间 `App.*`。

---

## 五、版本控制策略

### 5.1 版本号（语义化三段）

```
vMAJOR.MINOR.PATCH
```

| 段 | 何时递增 | 本项目历史示例 |
|----|----------|----------------|
| MAJOR | 架构/UI 框架重写、破坏性契约变更 | v1.0(P0-3) → v2.0(pywebview+P1) |
| MINOR | 新增功能、非破坏性增强 | v2.0 → v2.1(审核修订) → v2.2(日志) → v2.3(功能整合) |
| PATCH | 缺陷修复、无行为变化的调整 | — |

### 5.2 提交规范

- 提交信息格式：`<类型>(<模块>): <中文简述>`；
  类型：`feat / fix / docs / test / refactor / chore`；
  示例：`feat(transfer): 新增手动 IP 直连发送入口`；
- 一个提交只做一件事；文档与代码同提交或紧随提交；
- 禁止直接提交到主分支的破坏性改动；大功能走分支 → 合并前跑发布门禁（§3.3）。

### 5.3 发布流程

```
1. 冻结需求（该版本卡片全部关闭）
2. 跑全量回归（单测 + UI 冒烟 + JS 语法检查）
3. 更新 技术文档.md CHANGELOG（新版本条目：功能/修复/测试/已知问题）
4. 功能设计文档同步（新章节或修订）
5. 打版本标签 + 归档需求卡片状态
```

---

## 六、文档更新流程

### 6.1 文档职责矩阵

| 文档 | 职责 | 何时必须更新 |
|------|------|--------------|
| [功能设计文档.md](./功能设计文档.md) | 功能需求与设计（给产品/用户看） | 新功能定稿、设计变更 |
| [技术文档.md](./技术文档.md) | 架构/协议/API/配置/CHANGELOG（给开发排障） | 任何代码合并 |
| [使用文档.md](./使用文档.md) | 用户操作手册 | 页面交互变更、配置项新增 |
| 本框架文档 | 流程规范本身 | 流程调整时 |
| 需求卡片 | 单条需求全生命周期 | 状态变更（采纳/排期/完成/拒绝） |

> 历史过程记录（UI 优化对比、功能审查、中断任务盘点、历史方案）统一归档至
> `docs/archive/`，不再作为活跃文档维护；其结论已沉淀进上述文档与代码。

### 6.2 更新触发规则

- **代码合并前**：技术文档 CHANGELOG + js_api 清单/端口表/配置表 若有变动必须同步；
- **新页面/新入口**：功能设计文档对应章节补「使用方法」段落；
- **协议/帧格式变动**：技术文档 §二 端口与协议 + 功能设计文档协议段；
- **配置项增删**：两文档配置表同步；配置默认值变更按 MINOR 记；
- **修复**：技术文档 CHANGELOG PATCH 条目（一句话 + 影响模块）；
- **文档与代码不一致视为缺陷**，计入发布门禁。

### 6.3 维护节奏

- 每版本发布日统一校对全部文档（链接、版本号、测试命令、配置表）；
- 文档头部标注版本与日期；引用其他文档用相对链接；
- 大版本切换（MAJOR）重审全部章节是否仍成立，淘汰过时内容。

---

## 七、模板附录

- [需求卡片模板](#22-需求卡片模板必填)（§2.2）
- 版本发布检查单（§5.3 / §3.3）
- 新增模块检查清单（§4.2）
- RICE 评分表（§3.2）
